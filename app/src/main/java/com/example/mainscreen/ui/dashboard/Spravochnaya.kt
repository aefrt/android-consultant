package com.example.mainscreen.ui.dashboard

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Switch
import com.example.mainscreen.R

class Spravochnaya : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_spravochnaya)
       /* val standardSwitch: Switch = findViewById(R.id.switch_compat)

        standardSwitch.setOnCheckedChangeListener { buttonView, isChecked ->
            if (isChecked) {
                println("Standard Switch is on")
            } else {
                println("Standard Switch is off")
            }
        }*/
    }
}